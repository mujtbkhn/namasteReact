const Profile = (props) => {
    return (
        <div>

            <h1>This is Profile Functional Component</h1>
            <h2>Name: {props.name}</h2>

        </div>
    )
}

export default Profile